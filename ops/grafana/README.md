Import these into Grafana when (and if) you spin one up: use Grafana's "Import dashboard" UI (Dashboards → Import → Upload JSON file) and select a `${DS_PROMETHEUS}` datasource that points at your Prometheus instance scraping `http://127.0.0.1:8090/metrics` via an SSH tunnel or internal network. See also `plan/06b-dashboard.md` for the human-facing UI — after 06b lands, the Grafana stack is optional (machine-grade ops view only).
